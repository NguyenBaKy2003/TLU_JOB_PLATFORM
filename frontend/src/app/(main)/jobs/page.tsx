import { redirect } from "next/navigation";

export default function JobsPage() {
  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center">
      <p className="text-gray-400 text-sm">Jobs page — coming soon</p>
    </div>
  );
}